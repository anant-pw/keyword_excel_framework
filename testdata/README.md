This folder is for actual test INPUT data - files a test uploads or reads
during a run (sample PDFs, CSVs, images used in an upload flow, etc).

It is NOT for locators or credentials - those live in properties/*.properties
now, referenced via $name from the test sheet. See testsheets/TestSuite.xlsx
and properties/login.properties for the pattern.
